# Server configuration
PORT=8080
BASE_URL=http://localhost:8080

# Logging format: "text" (human readable) or "json" (structured)
# Auto-detects TTY if not set
# LOG_FORMAT=text

# Database - libsql/Turso
# Local development: file:refyne.db
# With turso dev: http://127.0.0.1:8080
DATABASE_URL=file:refyne.db

# Turso Cloud (optional - enables embedded replica sync)
# Create a free database at https://turso.tech
# TURSO_URL=libsql://your-db-your-org.turso.io
# TURSO_AUTH_TOKEN=your-auth-token

# Clerk Authentication (required)
# Get the issuer URL from Clerk Dashboard -> API Keys -> Advanced
# For testing: use email+clerk_test@example.com with code 424242
CLERK_ISSUER_URL=https://xxx.clerk.accounts.dev

# Encryption key (optional - derived from JWT_SECRET if not set)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=

# JWT Secret (for signing internal tokens)
JWT_SECRET=

# Stripe (for billing)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Service LLM keys (for users using credits)
# At least one is required for credit-based usage
SERVICE_ANTHROPIC_KEY=
SERVICE_OPENAI_KEY=
SERVICE_OPENROUTER_KEY=

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000

# Admin panel enabled
ADMIN_ENABLED=true
