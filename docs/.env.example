# Clerk Authentication (for JWT injection in API playground)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx

# API URL for OpenAPI spec fetching (falls back to production API)
NEXT_PUBLIC_API_URL=https://api.refyne.uk

# Main site URL for cross-site links
NEXT_PUBLIC_MAIN_SITE_URL=https://refyne.uk
