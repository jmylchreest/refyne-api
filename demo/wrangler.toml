# Cloudflare Pages configuration for refyne-demo
name = "refyne-demo"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Custom domain will be configured in Cloudflare dashboard
# after initial deployment: demo.refyne.uk
